# Block direct web access to the raw posts data.
# PHP still reads it from the filesystem — only browser/HTTP access is denied.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
